database:
{
	# Enter Your oracle database connection data below
        service = "database_service_name";
        username = "database_user_name";
        password = "database_user_password";
        nls_lang = "NLS_LANG=.UTF8";
};

geoserver:
{
	connectStr = "dbname=gis user=gis_user password=gis_pw host=smartmet-test port=5444";
};

## Uncomment and use real values if proxy is required for dowloading XML schemas (no other
## use of proxy servers yet)
#httpProxy="http://wwwproxy.example.org:8080";
#noProxy="127.0.0.1,127.,127.*,localhost,.example.org,192.168.*.*";
